local colors = require('kanagawa.colors')

return {
    cursorline = colors.crystalBlue,
    matchParen = colors.springGreen,
    diffAdded = colors.springGreen,
    diffChanged = colors.surimiOrange,
    diffDeleted = colors.autumnRed,
    error = colors.autumnRed,
    warning = colors.autumnYellow,
    info = colors.lightBlue
}
